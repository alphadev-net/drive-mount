buildscript {
    repositories {
        jcenter()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:0.12.2'
    }
}
apply plugin: 'java'

dependencies {
    compile project(':app')
    compile 'junit:junit:4.12-beta-1'
}